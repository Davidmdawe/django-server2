function pageLoadEvents() {
    switch(document.title) {
        case 'User Profile Settings | Harmony Systems':
            makeVisible(document.getElementById('sidebarLinksSettings'), true);
            break;
        
    }
}